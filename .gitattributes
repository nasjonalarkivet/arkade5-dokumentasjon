# Normalize line endings: store as LF in the repo, convert per-platform on checkout.
* text=auto

# Windows batch files must be CRLF to run reliably under cmd.exe.
*.bat text eol=crlf

# Binary assets — never touch line endings.
*.png binary
*.ico binary
